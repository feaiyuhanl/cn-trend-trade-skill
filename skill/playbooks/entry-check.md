# Playbook: entry-check（入场快检）

适用：`session_mode=new_entry`，用户只关心能否开仓。

## Lenses（精简）

`market-filter` → `trend-strength` → `trend-phase` → `entry-signals` → `position-management` → `discipline`

可跳过详尽的 exit 规划（`exit_plan` 填最小止损框架即可）。

其余同 [full-analysis.md](full-analysis.md)。
